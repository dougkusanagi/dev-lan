import { Copy, ExternalLink, Lock, RefreshCw, ShieldAlert } from 'lucide-react';
import type { ProjectInfo } from '../../types';

export function ProjectHeader({ project, tab, onTab, onOpen, onCopy, onCopyPath, onToggleTLS, onReload }: { project: ProjectInfo; tab: 'overview'|'logs'; onTab: (t: 'overview'|'logs') => void; onOpen: () => void; onCopy: () => void; onCopyPath: () => void; onToggleTLS: () => void; onReload: () => void }) {
  return <header className="project-header"><div className="project-context"><div><span>PROJETO</span><h1>{project.name}</h1></div><span className={`inline-status ${project.status}`}>{project.status === 'ready' ? 'Pronto' : project.status}</span></div><div className="address-bar"><button className={project.tlsEnabled ? 'address-tls active' : 'address-tls'} title={project.tlsEnabled ? 'Desativar SSL/HTTPS' : 'Ativar SSL/HTTPS'} aria-label={project.tlsEnabled ? 'Desativar SSL/HTTPS' : 'Ativar SSL/HTTPS'} onClick={onToggleTLS}>{project.tlsEnabled ? <Lock size={16}/> : <ShieldAlert size={16}/>}</button><code>{project.url}</code><span className="framework-badge">{project.framework || project.effectiveMode}</span><div className="address-actions"><button title="Abrir URL" aria-label="Abrir URL" onClick={onOpen}><ExternalLink size={17}/></button><button title="Copiar URL" aria-label="Copiar URL" onClick={onCopy}><Copy size={17}/></button><button title="Recarregar infraestrutura" aria-label="Recarregar infraestrutura" onClick={onReload}><RefreshCw size={17}/></button></div></div><div className="project-tabs"><button className={tab === 'overview' ? 'active' : ''} onClick={() => onTab('overview')}>Visão geral</button><button className={tab === 'logs' ? 'active' : ''} onClick={() => onTab('logs')}>Logs</button><div className="project-path"><code title={project.path}>{project.path}</code><button title="Copiar caminho do projeto" aria-label="Copiar caminho do projeto" onClick={onCopyPath}><Copy size={14}/></button></div></div></header>;
}
